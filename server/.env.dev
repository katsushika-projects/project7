COMPOSE_PROJECT_NAME=project7

DB_ENGINE=django.db.backends.postgresql
DB_NAME=postgres
DB_USER=postgres
POSTGRES_PASSWORD=postgres-password
DB_HOST=postgres
DB_PORT=5432

DJANGO_SECRET_KEY='default-secret-key'
DJANGO_STATIC_URL='static/'         # 静的ファイルにアクセスするためのURLのパス
DJANGO_STATIC_ROOT='static/'        # 静的ファイルの保存先のパス
DJANGO_MEDIA_URL='media/'           # メディアファイルにアクセスするためのURLのパス
DJANGO_MEDIA_ROOT='media/'          # メディアファイルの保存先のパス
